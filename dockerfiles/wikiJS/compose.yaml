services:
  wiki:
    image: ghcr.io/requarks/wiki:2
    environment:
      DB_TYPE: sqlite
      DB_FILEPATH: '/data/db.sqlite'
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - wiki-js-data:/data

volumes:
  wiki-js-data:

